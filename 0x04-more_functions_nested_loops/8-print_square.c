#include "main.h"
/**
 *print_square - prints square followed by new line
 *@size: square size
 */
void print_square(int size);
{
  int i;
  int j;
  while (n > 0)
    {
      for (i = 1; i < = n; i++)
	{

	  for (j = 0; j < = n; j++)
	    {

	      _putchar('#');

	    }
	   _putchar('\n');
	}
    }
  _putchar('\n');
  return (0);

}
