include "main.h"
/**
 *print_diagonal - prints diagonal followed by newline
 */
void print_square(int size)
{
  int i;
  int j;
  while (n > 0)
    {
      for (i = 0 ; i < = n ; i++)
	{

	  for (j = 1 ; j < = i ; j++)
	    {

	      _putchar(' ');

	    }
	  _putchar('\\');
	  _putchar('\n');
	}
    }
  _putchar('\n');
  return (0);

}
