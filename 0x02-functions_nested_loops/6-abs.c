#include "main.h"
/**
 *_abs - Prints the absolute value of  a number
 *@i: the number to be evaluated
 * Return: Always 0 (Success)
 */
int _abs(int i)
{
  if (i >= 0)
    return (i);
  else if (i < 0)
    return (-i);
}
