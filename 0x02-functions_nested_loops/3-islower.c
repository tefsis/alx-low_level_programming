#include "main.h"
#include <stdio.h>
/**
 * print_alphabet_x10 -  prints the alphabet x10
 *
 * Return: Always 0.
 */

int _islower(int c);
{
  int c;
  if (c >= 'a' && c <= 'z')
    return (1);
	    else
	      return (0)
	
    
      
    
}
