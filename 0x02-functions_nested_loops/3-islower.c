#include "main.h"
/**
 * print_alphabet - Prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int _isalpha(int c)
{
  int c;
  if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
    return (1)
    else
      return (0)
	}
