#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - prints the various sizes of data
 *
 *Return: 0 if true
 */
int _islower(int c);
{
int c;
if (c <= 'a' && c <= 'z')
return (1);
else
return (0);
}
int main (void)
{
   return (0);
}
}
