#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - prints the various sizes of data
 *
 *Return: 0 if true
 */
void print_alphabet(void);
{
char chr;
for (chr = 'a'; chr <= 'z'; chr++)
putchar(chr);
putchar('\n');
}
int main (void)
{
return (0);
}
